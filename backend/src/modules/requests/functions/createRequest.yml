createRequest:
  handler: src/modules/requests/handlers/createRequest.handler
  events:
    - httpApi:
        path: /api/requests
        method: post
        authorizer:
          name: clerkJwtAuthorizer
  environment:
    GEMINI_API_KEY: ${env:GEMINI_API_KEY}
  timeout: 30
  memorySize: 512
